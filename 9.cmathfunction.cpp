#include <cmath>
#include <iostream>

int main() {

    double x = 3.14;
    double y = 2.25;
    double z;

    // some simple and basic math function

    // z = std::max(x, y); find which is bigger
    // z = std::min(x, y); find which is smaller
    // z = std::pow(x, y); pangkat bilangan
    // z = sqrt(9); akar dari value
    // z = abs(-3); convert absolute
    // z = round(3.50); round normal
    // z = floor(3.99);
    z = ceil(3.10);


    std::cout << z << "\n";
    return 0;
}
